//
// Created by Alisa Soroka on 6/2/17.
//

#include "../../rtv1.h"

void 		change_pos(t_obj *obj)
{
//	if (obj->type_obj == 0)
//	{
		obj->f->change_pos = 0;
//		free(obj->intersects);
//		free(obj->is_intersects);
//		obj->intersects = (t_v *)malloc(sizeof(t_v) * (HEIGHT * WIDTH + 1));
//		obj->is_intersects = (int *)malloc(sizeof(int) * (HEIGHT * WIDTH + 1));
//	}
}